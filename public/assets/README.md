# Assets

Place the OFF_LIMIT TECH logo here.

Recommended filename: `olt-logo.png` or `olt-logo.svg`

Then update the logo placeholder in:
- src/pages/off-limit-tech.astro (OLT Hero section)

Replace the `.olt-logo-placeholder` div with:
```html
<img src="/assets/olt-logo.png" alt="OFF_LIMIT TECH" style="width:72px;height:72px;border-radius:14px;" />
```
